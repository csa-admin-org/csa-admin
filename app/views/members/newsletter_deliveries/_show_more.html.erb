<turbo-frame id="show-more">
  <div class="md:w-none mt-6 w-full text-center">
    <%= link_to t(".link"), members_newsletter_deliveries_path(offset: @next_offset, format: :turbo_stream),
      class: "btn btn-sm",
      data: { turbo_prefetch: false } %>
  </div>
</turbo-frame>
