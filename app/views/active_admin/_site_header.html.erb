<div class="admin-bar">
  <div class="admin-bar-inner" data-controller="nav-toggle">
    <div class="admin-menu-toggle-wrap">
      <button
        type="button"
        class="admin-menu-toggle"
        data-action="nav-toggle#toggle"
        aria-expanded="false"
      >
        <%= icon "menu", class: "admin-menu-toggle-icon" %>

        <span class="sr-only">
          <%= t("accessibility.active_admin.open_main_menu") %>
        </span>
      </button>
    </div>

    <nav class="admin-nav" id="navbar-dropdown">
      <ul class="admin-nav-list">
        <% current_menu.items(self).each do |item| %>
          <% children = item.items(self).presence %>
          <% label = menu_label_with_badge(item) %>
          <% plain_label = item.label(self) %>

          <li class="admin-nav-item" data-item-id="<%= item.id %>">
            <% if children %>
              <% first_child = children.first %>
              <% parent_quick_access = item.id != "other" && first_child&.url(self) %>
              <% parent_current = children.any? { |j| current_menu_item?(j) } %>
              <% parent_classes = "admin-nav-parent#{' is-current' if parent_current}" %>
              <% if parent_quick_access %>
                <%= link_to first_child.url(self),
                  id: "dropdownHoverButton-#{item.id}",
                  class: parent_classes,
                  **(current_menu_item?(first_child) ? { "aria-current" => "page" } : {}) do %>
                  <%= label %>
                  <%= icon "chevron-down", class: "admin-nav-chevron" %>
                <% end %>
              <% else %>
                <button
                  id="dropdownHoverButton-<%= item.id %>"
                  class="<%= parent_classes %>"
                >
                  <%= label %>
                  <%= icon "chevron-down", class: "admin-nav-chevron" %>
                </button>
              <% end %>
              <%= tag.ul(
                id: item.id,
                role: "list",
                class: "admin-nav-dropdown",
                "aria-labelledby": "dropdownHoverButton-#{item.id}",
                data: item.html_options[:data].to_h
              ) do %>
                <% children.each do |j| %>
                  <li data-item-id="<%= j.id %>">
                    <%= link_to j.label(self), j.url(self), j.html_options.merge(class: "admin-nav-dropdown-link#{' is-current selected' if current_menu_item?(j)}") %>
                  </li>
                <% end %>
              <% end %>
              <div class="admin-nav-mobile-heading">
                <%= plain_label %>
              </div>
              <ul class="admin-nav-mobile-list">
                <% children.each do |j| %>
                  <li data-item-id="<%= j.id %>">
                    <%= link_to menu_label_with_badge(j, badge_class: "menu-inline-badge"), j.url(self), j.html_options.merge(class: "admin-nav-dropdown-link#{' is-current selected' if current_menu_item?(j)}") %>
                  </li>
                <% end %>
              </ul>
            <% elsif url = item.url(self) %>
              <%= link_to label, url, item.html_options.merge(class: "admin-nav-link#{' is-current' if current_menu_item?(item)}").merge(current_menu_item?(item) ? { "aria-current" => "page" } : {}) %>
            <% else %>
              <%= label %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </nav>

    <div class="admin-bar-actions">
      <%= render "search/modal" %>

      <% if Update.unread_count(current_active_admin_user).positive? %>
        <a
          href="<%= updates_path %>"
          class="admin-header-action"
          data-turbo-prefetch="false"
          title="<%= t(".updates") %>"
        >
          <%= icon "gift" %>

          <div class="admin-header-unread"></div>
        </a>
      <% end %>

      <a
        href="<%= support_path %>"
        class="admin-header-action"
        title="<%= t(".support") %>"
        data-turbo-prefetch="false"
      >
        <%= icon "life-buoy" %>
      </a>

      <a
        href="<%= analytics_path %>"
        class="admin-header-action"
        title="<%= t(".analytics") %>"
        data-turbo-prefetch="false"
      >
        <%= icon "chart-no-axes-combined" %>
      </a>

      <a
        href="<%= handbook_path %>"
        class="admin-header-action"
        title="<%= t(".handbook") %>"
      >
        <%= icon "book-open" %>
      </a>

      <% if can?(:read, Organization) %>
        <a href="<%= organization_path %>" class="admin-header-action">
          <%= icon "sliders-horizontal" %>
        </a>
      <% end %>

      <%= render "active_admin/user_menu" %>
    </div>
  </div>
</div>
