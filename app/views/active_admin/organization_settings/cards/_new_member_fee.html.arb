# frozen_string_literal: true

context.instance_eval do
  para organization_disabled_setting_section_hint(section), class: "description m-0 p-2 text-gray-500"

  attributes_table_for org do
    row(InvoiceItem.human_attribute_name(:description)) do
      org.new_member_fee_description.presence || organization_settings_missing_status_tag
    end
    row(InvoiceItem.human_attribute_name(:amount)) { organization_settings_currency_value(org.new_member_fee) }
  end
end
